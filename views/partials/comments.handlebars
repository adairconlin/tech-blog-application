<div>
    {{#each this}}
    <section>
        <div>
            {{comment_text}}
        </div>
        
        <div>
            by {{user.username}} on {{created_at}}
        </div>
    </section>
    {{/each}}
</div>